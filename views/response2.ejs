<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Resume Review Result</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
            background-color: #ffffff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
        }

        h1 {
            text-align: center;
            color: #333;
        }

        .message {
            background-color: #e8f5e9;
            padding: 10px;
            border: 1px solid #4CAF50;
            border-radius: 4px;
            margin-bottom: 20px;
            font-weight: bold;
            text-align: center;
        }

        .review {
            font-size: 16px;
            line-height: 1.6;
            color: #555;
            white-space: pre-wrap; /* To preserve whitespace formatting */
        }

        .score {
            font-weight: bold;
            font-size: 18px;
            color: #2e7d32;
            margin-bottom: 20px;
        }

        .reasoning {
            margin-top: 20px;
            padding: 10px;
            background-color: #f9fbe7;
            border-left: 5px solid #cddc39;
            border-radius: 4px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Resume Review Result</h1>
        <div class="message"><%= message %></div>
        <div class="review">
            <div class="score">Score: <%= review.match(/Score: (\d+)/)[1] %></div>
            <div class="reasoning"><%= review.replace(/Score: \d+\n\n/, '') %></div>
        </div>
    </div>
</body>
</html>
